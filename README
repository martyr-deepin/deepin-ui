Deepin UI Toolkit for Linux Deepin.

* Install:
  sudo apt-get install git
  sudo apt-get install python-setuptools python-gtk2-dev python-cairo-dev && sudo python setup.py install
  
* Use:
  python ./deepin-ui/demo.py
